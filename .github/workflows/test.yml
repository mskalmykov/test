name: github actions test
on:
  push:
    tags:
      - v[0-9].[0-9]+
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check environment
        run: |
          printenv | grep GIT
